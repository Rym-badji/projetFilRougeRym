{% extends 'base.html.twig' %}

{% block title %}New Task{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new Task</h1>
        
        {{ include('task/formTask.html.twig') }}
        
        <a href="{{ path('app_projet_show', {'id': task.projet.id}) }}" class="btn btn-primary">Retour au projet</a>
    </div>
{% endblock %}
