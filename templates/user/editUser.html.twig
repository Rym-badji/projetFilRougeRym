{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit User</h1>

        {{ include('user/formUser.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_user_index') }}">back to list</a>

        {{ include('user/deleteUser.html.twig') }}
    </div>
{% endblock %}
