{% extends 'base.html.twig' %}

{% block title %}Edit Adress{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center align-items-center" style="height: 60vh;">
        <div style="width: 100%; max-width: 600px;">
            <div class="card-body"style="padding: 2rem;">

                <h1 class="h4 text-center mt-3">Edit Adress</h1>
                {{ include('adress/formAdress.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>
    <div class="text-center">
        <a href="{{ path('app_adress_index') }}">back to list</a>
        {{ include('adress/deleteAdress.html.twig') }}
    </div>
{% endblock %}
