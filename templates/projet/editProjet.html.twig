{% extends 'base.html.twig' %}

{% block title %}Edit Projet{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center align-items-center" style="height: 80vh;">
        <div style="width: 100%; max-width: 600px;">
            <div class="card-body"style="padding: 2rem;">

                <h1 class="h4 text-center">Edit Projet</h1>
                {{ include('projet/formProjet.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>
    <div class="text-center">
        <a href="{{ path('app_projet_index') }}">back to list</a>
        {{ include('projet/deleteProjet.html.twig') }}
    </div>
{% endblock %}
